fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
jinja2==3.1.6
python-multipart==0.0.32
python-dotenv==1.0.1
httpx==0.28.1
anthropic
pypdf
# Base de fusos horários (horário de Brasília nos painéis). Algumas
# imagens Linux enxutas não trazem, e o zoneinfo precisa dela.
tzdata

# Só necessário se um dia migrar os manuais pra um bucket S3 (AWS) --
# ver MANUAIS_STORAGE no .env. Local com pasta no PC não precisa.
# boto3

# Só necessário em produção, conectando em Postgres de verdade.
# Local (SQLite) não precisa -- se falhar ao instalar, ignore.
# Sem versão travada de propósito: versões antigas (ex: 2.9.9) não têm
# pacote pré-compilado pro Python usado no Railway, e tentam compilar
# do zero (o que falha lá por falta de ferramenta de build).
psycopg2-binary
